# =============================================================================
# MySmartNotes — .env Example
#
# WHAT GOES HERE:
#   - Host/connection details (DB, Redis, server bind)
#   - Sensitive credentials (SMTP password, AI API keys, Firebase keys)
#   - Admin bootstrap email
#
# WHAT DOES NOT GO HERE:
#   - Non-sensitive runtime defaults → config/app.config.json  (auto-created)
#   - Generated secrets            → secrets.env               (auto-created)
#
# HOW TO USE:
#   cp .env.example .env
#   # Fill in your values, then start the app — secrets.env and
#   # config/app.config.json will be created automatically on first run.
# =============================================================================

# ============================================================
# Database Connection (host/user — password is auto-generated into secrets.env)
# ============================================================
DB_USER=mysmartnotes
DB_HOST=localhost
DB_PORT=5432
DB_NAME=mysmartnotes

# ============================================================
# Redis
# ============================================================
REDIS_URL=redis://localhost:6379/0

# ============================================================
# Server
# ============================================================
# HOST / API_PORT: what uvicorn (the FastAPI process) binds to inside the container.
HOST=0.0.0.0
API_PORT=8000

# PUBLIC_PORT: the host port that nginx (or docker-compose) exposes to the outside world.
PUBLIC_PORT=8000

# ============================================================
# Admin Bootstrap Account
# ============================================================
# If set, this account is elevated to Admin on startup if it already exists.
ADMIN_EMAIL=admin@example.com

# ============================================================
# SMTP Email Configuration
# ============================================================
SMTP_HOST=smtp.gmail.com
SMTP_PORT=587
SMTP_USER=your-email@gmail.com
SMTP_PASSWORD=your-app-password
SMTP_SENDER_NAME=MySmartNotes
SMTP_TLS=True

# ============================================================
# Global AI Configuration (3-Tier Fallback)
# ============================================================
# Tier 1 — Primary
GLOBAL_AI_TIER1_PROVIDER=gemini
GLOBAL_AI_TIER1_MODEL=models/gemma-4-31b-it
GLOBAL_AI_TIER1_API_KEY=
GLOBAL_AI_TIER1_REASONING_LEVEL=high

# Tier 2 — Secondary
GLOBAL_AI_TIER2_PROVIDER=gemini
GLOBAL_AI_TIER2_MODEL=models/gemma-4-26b-a4b-it
GLOBAL_AI_TIER2_API_KEY=
GLOBAL_AI_TIER2_REASONING_LEVEL=high

# Tier 3 — Local fallback (Ollama)
GLOBAL_AI_TIER3_PROVIDER=ollama
GLOBAL_AI_TIER3_MODEL=gemma4:e2b
GLOBAL_AI_TIER3_API_KEY=
GLOBAL_AI_TIER3_REASONING_LEVEL=low
GLOBAL_AI_TIER3_BASE_URL=http://localhost:11434

# ============================================================
# Firebase Configuration
# Get values from Firebase Console → Project Settings → Your Apps
# ============================================================
FIREBASE_API_KEY=your-firebase-api-key
FIREBASE_AUTH_DOMAIN=your-project-id.firebaseapp.com
FIREBASE_PROJECT_ID=your-project-id
FIREBASE_STORAGE_BUCKET=your-project-id.firebasestorage.app
FIREBASE_MESSAGING_SENDER_ID=your-sender-id
FIREBASE_APP_ID=your-app-id
FIREBASE_MEASUREMENT_ID=your-measurement-id
