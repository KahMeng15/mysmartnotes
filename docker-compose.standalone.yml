services:
  db:
    image: postgres:15-alpine
    container_name: velonote_db
    restart: unless-stopped
    environment:
      POSTGRES_USER: ${DB_USER:-velonote}
      POSTGRES_PASSWORD: ${DB_PASSWORD}
      POSTGRES_DB: ${DB_NAME:-velonote}
    volumes:
      - db_data:/var/lib/postgresql/data
    ports:
      - "5432:5432"
    networks:
      - velonote-net
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U $$POSTGRES_USER"]
      interval: 10s
      timeout: 5s
      retries: 5

  redis:
    image: redis:alpine
    container_name: velonote_redis
    restart: unless-stopped
    ports:
      - "6379:6379"
    networks:
      - velonote-net
    healthcheck:
      test: ["CMD", "redis-cli", "ping"]
      interval: 10s
      timeout: 5s
      retries: 5

  api:
    platform: linux/amd64
    image: ghcr.io/kahmeng15/velonote:dev
    container_name: velonote_api
    command: ["python", "-m", "uvicorn", "app.main:app", "--host", "0.0.0.0", "--port", "${API_PORT:-8000}"]
    depends_on:
      db: { condition: service_healthy }
      redis: { condition: service_healthy }
    env_file:
      - .env
      - secrets.env
    environment:
      - DB_HOST=db
      - REDIS_URL=redis://redis:6379/0
      - USER_ID=${PUID:-568}
      - GROUP_ID=${PGID:-568}
    volumes:
      - ./data:/app/data
      - ./logs:/app/logs
      - ./config:/app/config
    ports:
      - "${PUBLIC_PORT:-8000}:${API_PORT:-8000}"
    restart: unless-stopped
    networks:
      - velonote-net

  worker:
    platform: linux/amd64
    image: ghcr.io/kahmeng15/velonote:dev
    container_name: velonote_worker
    command: ["python", "-m", "app.worker_main"]
    depends_on:
      db: { condition: service_healthy }
      redis: { condition: service_healthy }
    env_file:
      - .env
      - secrets.env
    environment:
      - DB_HOST=db
      - REDIS_URL=redis://redis:6379/0
      - USER_ID=${PUID:-568}
      - GROUP_ID=${PGID:-568}
    volumes:
      - ./data:/app/data
      - ./logs:/app/logs
      - ./config:/app/config
    restart: unless-stopped
    networks:
      - velonote-net

volumes:
  db_data:

networks:
  velonote-net:
    driver: bridge
